// Code generated by forge generate. DO NOT EDIT.

package primitives

// SelectOption represents a key-value pair for select inputs.
type SelectOption struct {
	Value string
	Label string
}

// FormField renders a labeled form field wrapper with optional error message.
// When errMsg is non-empty, a red error paragraph is rendered below the input.
templ FormField(label, name, errMsg string) {
	<div class="flex flex-col gap-1">
		<label for={ name } class="text-sm font-medium text-gray-700">{ label }</label>
		{ children... }
		if errMsg != "" {
			<p class="text-sm text-red-600" data-testid={ "error-" + name }>{ errMsg }</p>
		}
	</div>
}

// TextInput renders a text input with Datastar signal binding.
templ TextInput(name, value, placeholder string) {
	<input
		id={ name }
		type="text"
		name={ name }
		value={ value }
		placeholder={ placeholder }
		data-bind={ name }
		class="w-full rounded border border-gray-300 px-2 py-1 text-sm focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500"
	/>
}

// TextArea renders a multi-line text input with Datastar signal binding.
templ TextArea(name, value, placeholder string) {
	<textarea
		id={ name }
		name={ name }
		placeholder={ placeholder }
		data-bind={ name }
		rows="4"
		class="w-full rounded border border-gray-300 px-2 py-1 text-sm focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500"
	>{ value }</textarea>
}

// DecimalInput renders a numeric input for decimal values with Datastar signal binding.
templ DecimalInput(name, value, placeholder string) {
	<input
		id={ name }
		type="number"
		name={ name }
		value={ value }
		placeholder={ placeholder }
		step="0.01"
		data-bind={ name }
		class="w-full rounded border border-gray-300 px-2 py-1 text-sm focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500"
	/>
}

// SelectInput renders a select element with Datastar signal binding.
templ SelectInput(name, selected string, options []SelectOption) {
	<select
		id={ name }
		name={ name }
		data-bind={ name }
		class="w-full rounded border border-gray-300 px-2 py-1 text-sm focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500"
	>
		for _, opt := range options {
			if opt.Value == selected {
				<option value={ opt.Value } selected>{ opt.Label }</option>
			} else {
				<option value={ opt.Value }>{ opt.Label }</option>
			}
		}
	</select>
}

// RelationSelect renders a select with an empty "-- Select --" first option and Datastar signal binding.
// Use this for foreign-key / relation fields where no selection is a valid initial state.
templ RelationSelect(name, selected string, options []SelectOption) {
	<select
		id={ name }
		name={ name }
		data-bind={ name }
		class="w-full rounded border border-gray-300 px-2 py-1 text-sm focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500"
	>
		<option value="">-- Select --</option>
		for _, opt := range options {
			if opt.Value == selected {
				<option value={ opt.Value } selected>{ opt.Label }</option>
			} else {
				<option value={ opt.Value }>{ opt.Label }</option>
			}
		}
	</select>
}
