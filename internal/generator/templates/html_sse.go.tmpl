// Code generated by forge generate. DO NOT EDIT.

package sse

import (
	"fmt"

	datastar "github.com/starfederation/datastar-go/datastar"
)

// MergeFragment sends a templ component as a DOM fragment patch to the browser.
// This is a semantic wrapper around sse.PatchElementTempl.
func MergeFragment(sse *datastar.ServerSentEventGenerator, component datastar.TemplComponent) error {
	return sse.PatchElementTempl(component)
}

// Redirect sends a browser redirect via Datastar SSE.
// This is a semantic wrapper around sse.Redirect.
func Redirect(sse *datastar.ServerSentEventGenerator, url string) error {
	return sse.Redirect(url)
}

// RedirectTo sends a printf-style browser redirect via Datastar SSE.
// This is a semantic wrapper around sse.Redirectf.
func RedirectTo(sse *datastar.ServerSentEventGenerator, format string, args ...any) error {
	return sse.Redirect(fmt.Sprintf(format, args...))
}
